import * as React from 'react';
import Box from '@mui/material/Box';
import Button from '@mui/material/Button';

export default function ButtonSizes() {
  return (
    <Box>
      <div>
        <Button variant="contained" size="Raza mediana">
        Neruth
        </Button>
      </div>
    </Box>
  );
}